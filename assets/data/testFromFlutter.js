
	var isPremiumVersion = "false";    // ถ้าใช้บน PC เพราะเรียกจาก Android  ไม่ได้ ค่าที่ส่งเข้ามา คือ  true  หรือ  false

	// รับข้อมูลจาก flutter
function fromFlutter(newTitle) {
                document.getElementById("abc").innerHTML = newTitle;
                myFunction(newTitle)
             }

function checkAns(regn, ans) {
alert("regn: " + regn +", ans: " + ans)
}
function myFunction(sss){

document.getElementById("abcdef").innerHTML = sss;
}


